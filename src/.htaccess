# Live ORCL WebApp cache control and SSL cert

<FilesMatch "\.(css|flv|gif|htm|html|ico|jpe|jpeg|jpg|js|mp3|mp4|png|pdf|swf|txt)$">
    <IfModule mod_expires.c>
        ExpiresActive Off
    </IfModule>
    <IfModule mod_headers.c>
        FileETag None
        Header unset Cache-Control
        Header unset Last-Modified
        Header set Cache-Control "max-age=86400, public, must-revalidate"
    </IfModule>
</FilesMatch>



 RewriteEngine On
# RewriteCond %{SERVER_PORT} 80
# RewriteCond %{HTTP_HOST} ^(www\.)?clientmap.orcl.co.uk
# RewriteRule ^(.*)$ https://www.clientmap.orcl.co.uk/$1 [R,L]

# redirect to http subdomain
#RewriteCond %{HTTPS} on
#RewriteCond %{HTTP_HOST} ^((?!www).+\.clientmap.orcl\.co.uk)$ [NC]
#RewriteRule ^ http://%1%{REQUEST_URI} [R=301,L]

# redirect http to https all domain
RewriteCond %{HTTPS} off
RewriteRule (.*) https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]
